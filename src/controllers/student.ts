import express, { Request, Response } from "express";

// import { createStudent, deleteStudent, editStudent, fetchSingleStudent, fetchAllStudents } from "../services/booking";

export const studentController = express.Router();